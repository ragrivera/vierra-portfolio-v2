import { Placeholder } from '../components/Placeholder';

export function CareerList() {
  return (
    <Placeholder
      title="Career"
      note="List + edit form for career entries (slug, fromYear, toYear, role, org, tag, color, description, stats[], sort order). Wires to /admin/career endpoints."
    />
  );
}
