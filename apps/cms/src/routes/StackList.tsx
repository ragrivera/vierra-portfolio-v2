import { Placeholder } from '../components/Placeholder';

export function StackList() {
  return (
    <Placeholder
      title="Stack"
      note="List + edit form for stack categories (category label, items[], sort order). Wires to /admin/stack endpoints."
    />
  );
}
